const React = require('react');


const TestComponent = React.createClass({
    render: function () {
        return (
            <div>
                Test
            </div>
        );
    }
});

export default TestComponent;